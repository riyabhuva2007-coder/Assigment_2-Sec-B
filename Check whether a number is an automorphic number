#include <stdio.h>
int main() {
    int num, square, temp, rem;
    int isAutomorphic = 1;

    printf("Enter a number: ");
    scanf("%d", &num);

    square = num * num;
    temp = num;

    while (temp > 0) {
        rem = temp % 10;
        if (rem != square % 10) {
            isAutomorphic = 0;
            break;
        }
        temp /= 10;
        square /= 10;
    }

    if (isAutomorphic)
        printf("Automorphic number\n");
    else
        printf("Not an automorphic number\n");

    return 0;
}
